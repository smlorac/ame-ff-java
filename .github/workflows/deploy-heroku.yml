name: Deploy

on:
  push:
    branches:
      - feat/deploy # Changing the branch here would also work

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "bd03ce93-13f6-4398-a0fa-9e15010dbe6b"
          heroku_app_name: "cards-ameff" #Must be unique in Heroku
          heroku_email: "ms.caroline@hotmail.com"
          remote_branch: "feat/deploy"